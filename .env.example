# Optional. Leave unset to use localStorage (works immediately, single-device).
# Fill both in to switch to Supabase for real cross-device link sharing —
# see README.md "Cross-device sharing" section.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
